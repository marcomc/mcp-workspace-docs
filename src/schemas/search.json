{
  "tool": "search",
  "description": "Deterministic keyword search across workspace docs/code with stable ordering and optional file_glob filtering.",
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["query"],
    "properties": {
      "repo": {
        "type": "string",
        "enum": ["docs", "code", "both"],
        "description": "Logical repository name (docs, code, or both). Do not pass filesystem paths."
      },
      "query": {
        "type": "string",
        "minLength": 1
      },
      "file_glob": {
        "type": "string"
      },
      "limit": {
        "type": "integer",
        "minimum": 1
      }
    }
  },
  "output_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["result", "meta"],
    "properties": {
      "result": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["path", "line", "preview"],
          "properties": {
            "path": { "type": "string" },
            "line": { "type": "integer", "minimum": 1 },
            "preview": { "type": "string" }
          }
        }
      },
      "meta": {
        "type": "object",
        "additionalProperties": false,
        "required": ["repo", "duration_ms", "truncated"],
        "properties": {
          "repo": { "type": "string", "enum": ["docs", "code", "both"] },
          "duration_ms": { "type": "integer", "minimum": 0 },
          "truncated": { "type": "boolean" }
        }
      }
    }
  },
  "error_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["error"],
    "properties": {
      "error": {
        "type": "object",
        "additionalProperties": false,
        "required": ["code", "message", "details"],
        "properties": {
          "code": { "type": "string" },
          "message": { "type": "string" },
          "details": { "type": "object" }
        }
      }
    }
  }
}
